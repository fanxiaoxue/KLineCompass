<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.thinking.klinecompass"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelComActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>

        <meta-data
            android:name="design_width"
            android:value="800">
        </meta-data>
        <meta-data
            android:name="design_height"
            android:value="1280">
        </meta-data>

        <activity android:name="com.example.categoryinfo.ItemsActivity">
        </activity>
        <activity android:name="com.example.categoryinfo.NewsActivity">
        </activity>
        <activity android:name="com.example.categoryinfo.XiaDanActivity">
        </activity>
    </application>

</manifest>
